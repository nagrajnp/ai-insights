# NPrep · AI Sales Insight Panel

Prototype of the pre-call briefing panel for NPrep counsellors. Single self-contained HTML (Tailwind CDN + Poppins). Three stages × three tabs.

- `ai-sales-insight-panel.html` — the prototype
- `vercel.json` — rewrites `/` to the panel
